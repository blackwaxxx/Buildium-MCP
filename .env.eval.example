# DeepSeek key for running the MCP evaluations.
# The harness uses the Anthropic SDK, which reads these two vars; pointing
# ANTHROPIC_BASE_URL at DeepSeek redirects it with no code changes.
ANTHROPIC_BASE_URL=https://api.deepseek.com/anthropic
ANTHROPIC_API_KEY=
